package httpauth

// Role represents an interal role: a string mapped to an integer. Roles must
// be greater than zero.
type Role int

/*(a Authorizer) AddRole(role string, level int) {
    a.roles[role] = level
}*/
